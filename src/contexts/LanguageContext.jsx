// LanguageContext removed — translations are inlined in components (FR only)
// This file is kept as a stub to avoid import errors during transition.
export const LanguageProvider = ({ children }) => children;
export const useLanguage = () => ({ t: (k) => k });

